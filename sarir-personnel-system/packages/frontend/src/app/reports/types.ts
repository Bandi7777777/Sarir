export type Density = "comfort" | "dense" | "compact";

export type ColumnsVisibility = {
  category: boolean;
  count: boolean;
  description: boolean;
  trend: boolean;
  alert: boolean;
  subMetrics: boolean;
};
